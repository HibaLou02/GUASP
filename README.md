# GUASP — UI (HTML/CSS/Bootstrap)

## Démarrage

- Ouvrir `index.html` dans le navigateur.

## Structure

- `index.html`: page d'accès (connexion / création de compte)
- `assets/css/styles.css`: thème + styles de la page
- `assets/js/main.js`: interactions (tabs, rôle, validations simples)

